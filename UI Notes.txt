UI Notes:
Separate things into tabs(should be able to be opened and closed with keyboard):
Decoding(Decodercheck, timing integrity, decoded frames, decoded time stamps)
Calibration(maybe keep this one persistent)temporal median background, arena mask, arena and hole calibration

can probably collapse timing integrity readouts into the decoder check, and include previously commented variables an additional drop down. 
have arena mask preview and temporal median background be comebined into one viewer, be optional to be hidden, but exposed by default, and placed before arena calibration and hole geometry.

Generally move values or readouts to read on one line or in a table, generally moving stuff to take up whitespace more efficiently

Move or get rid of manual track corrections and and trial start messages, they add nothing. 

nose orientation preview should be a drop down

hole investigation preview can also be a drop down. 

